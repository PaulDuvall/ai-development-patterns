# Placeholder Source Directory

This `src/` directory exists so the sandbox Compose files can mount a **real, in-repo** path read-only at `/workspace/src`.

For real usage, replace this directory with (or bind-mount) the source tree you want an AI tool to read.
